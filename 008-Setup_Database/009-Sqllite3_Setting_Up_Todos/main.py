#!/usr/bin/python3
# Sqllite3 Setting Up Todos

"""
>>>> Connection to database using the sqllite3
        sqlite3 todos.db

        Listing all the tables.
            .schema
        
        Inserting a record.
            INSERT INTO todos (title, description, priority, complete) VALUES('Go to store', 'Pick up eggs', 5, False);
        
        Selecting all records.
            SELECT * FROM todos;

        Changing the mode for displaying rows in the tables.
            .mode column
            .mode markdown
            .mode box
            .mode table

>>>>
>>>>
>>>>
"""
